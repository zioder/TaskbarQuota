<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TaskbarQuota.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:tk="using:CommunityToolkit.WinUI.Controls"
    xmlns:vm="using:TaskbarQuota.ViewModels"
    xmlns:controls="using:TaskbarQuota.Controls"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  Padding="0,0,0,24"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Stretch">
        <StackPanel Spacing="6" HorizontalAlignment="Stretch">

            <TextBlock Text="Settings" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,8"/>

            <!-- Explains a refused pin, matching DashboardPage. A tooltip is no use here: the user has just
                 flicked the switch and watched it spring back, so the reason has to appear unprompted. -->
            <muxc:InfoBar x:Name="PinBlockedBar"
                          Severity="Warning"
                          Title="Can't pin this provider"
                          IsOpen="False"
                          IsClosable="True"
                          Margin="0,0,0,4"/>

            <TextBlock Text="Appearance" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,8,0,2"/>

            <tk:SettingsCard Header="Theme" Description="Choose how TaskbarQuota looks.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE771;"/>
                </tk:SettingsCard.HeaderIcon>
                <ComboBox x:Name="ThemeCombo" SelectionChanged="OnThemeChanged" MinWidth="160">
                    <ComboBoxItem Content="Use system setting" Tag="Default"/>
                    <ComboBoxItem Content="Light" Tag="Light"/>
                    <ComboBoxItem Content="Dark" Tag="Dark"/>
                </ComboBox>
            </tk:SettingsCard>

            <tk:SettingsCard Header="Widget layout" Description="Choose the compact usage layout (bars, percentages, or both).">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE71D;"/>
                </tk:SettingsCard.HeaderIcon>
                <ComboBox x:Name="WidgetModeCombo" SelectionChanged="OnWidgetModeChanged" MinWidth="180">
                    <ComboBoxItem Content="Bars only" Tag="BarsOnly"/>
                    <ComboBoxItem Content="Percentages only" Tag="PercentagesOnly"/>
                    <ComboBoxItem Content="Bars and percentages" Tag="BarsAndPercentages"/>
                </ComboBox>
            </tk:SettingsCard>

            <tk:SettingsCard Header="Percentage display" Description="Choose whether usage percentages show consumed or remaining quota.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE9D2;"/>
                </tk:SettingsCard.HeaderIcon>
                <ComboBox x:Name="PercentageModeCombo" SelectionChanged="OnPercentageModeChanged" MinWidth="160">
                    <ComboBoxItem Content="Consumed" Tag="Consumed"/>
                    <ComboBoxItem Content="Remaining" Tag="Remaining"/>
                </ComboBox>
            </tk:SettingsCard>

            <tk:SettingsCard Header="Hide widget when no AI app is focused"
                             Description="Fade the active provider's tile out when you switch to a browser or any other app, and bring it back when you return. Pinned providers always stay visible.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE890;"/>
                </tk:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="HideWhenUnfocusedToggle"
                              AutomationProperties.Name="Hide widget when no AI app is focused"
                              Toggled="OnHideWhenUnfocusedToggled"/>
            </tk:SettingsCard>

            <tk:SettingsCard Header="Where to show usage"
                             Description="Taskbar places the compact widget next to the tray. Floating uses a native Acrylic always-on-top window with the same layout. Scroll wheel adjusts its strength; right-click returns to the taskbar.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE8A7;"/>
                </tk:SettingsCard.HeaderIcon>
                <ComboBox x:Name="WidgetSurfaceCombo"
                          SelectionChanged="OnWidgetSurfaceChanged"
                          MinWidth="200"
                          AutomationProperties.Name="Where to show usage"
                          AutomationProperties.AutomationId="WidgetSurfaceCombo">
                    <ComboBoxItem Content="In the taskbar" Tag="Taskbar"/>
                    <ComboBoxItem Content="Floating always-on-top window" Tag="Floating"/>
                </ComboBox>
            </tk:SettingsCard>

            <tk:SettingsCard x:Name="TaskbarPlacementCard"
                             Header="Taskbar screens"
                             Description="Show the same usage on every taskbar, choose one screen, or place each provider on the screen where its window was last used.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE7F4;"/>
                </tk:SettingsCard.HeaderIcon>
                <ComboBox x:Name="TaskbarPlacementCombo"
                          SelectionChanged="OnTaskbarPlacementChanged"
                          MinWidth="200"
                          AutomationProperties.Name="Taskbar screens"
                          AutomationProperties.AutomationId="TaskbarPlacementCombo"/>
            </tk:SettingsCard>

            <tk:SettingsCard x:Name="FloatingOpacityCard"
                             Header="Floating acrylic strength"
                             Description="How opaque the native Acrylic tint is (lower values reveal more of the blurred desktop behind it).">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE790;"/>
                </tk:SettingsCard.HeaderIcon>
                <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                    <!-- Slider range is applied in code — XAML Minimum/Maximum on RangeBase is unsafe. -->
                    <Grid x:Name="FloatingOpacitySliderHost"
                          Width="180"
                          VerticalAlignment="Center"
                          AutomationProperties.Name="Floating acrylic strength"/>
                    <TextBlock x:Name="FloatingOpacityLabel"
                               VerticalAlignment="Center"
                               MinWidth="40"
                               Style="{StaticResource BodyTextBlockStyle}"/>
                </StackPanel>
            </tk:SettingsCard>

            <TextBlock Text="Providers" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,16,0,2"/>
            <tk:SettingsCard Header="Hide unavailable providers"
                             Description="Automatically hide providers that are not installed or signed in. You can still add them from the dashboard or below.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE7B8;"/>
                </tk:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="AutoHideUnavailableToggle"
                              AutomationProperties.Name="Hide unavailable providers"
                              Toggled="OnAutoHideUnavailableToggled"/>
            </tk:SettingsCard>

            <TextBlock Text="Choose which providers appear in the dashboard and usage widget."
                       Opacity="0.7"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,4"/>

            <StackPanel x:Name="ProviderSettingsPanel" Spacing="4"/>

            <TextBlock Text="Behavior" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,16,0,2"/>
            <tk:SettingsCard Header="Open at startup" Description="Start TaskbarQuota with Windows and show only the usage widget.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE7F4;"/>
                </tk:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="StartupToggle"
                              AutomationProperties.Name="Open at startup"
                              Toggled="OnStartupToggled"/>
            </tk:SettingsCard>

            <TextBlock Text="Notifications" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,16,0,2"/>
            <tk:SettingsCard Header="Quota alerts" Description="Show Windows notifications when usage crosses your configured thresholds.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xEA8F;"/>
                </tk:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="QuotaAlertsToggle"
                              AutomationProperties.Name="Enable quota alerts"
                              Toggled="OnQuotaAlertsToggled"/>
            </tk:SettingsCard>

            <tk:SettingsCard Header="Quota replenishment"
                             Description="Notify when available quota increases significantly or a usage window renews.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE7E7;"/>
                </tk:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="QuotaReplenishmentToggle"
                              AutomationProperties.Name="Notify when quota is replenished"
                              Toggled="OnQuotaReplenishmentToggled"/>
            </tk:SettingsCard>

            <tk:SettingsCard Header="Changes since last session"
                             Description="Notify after startup when quota increased since TaskbarQuota's last live reading. Requires quota replenishment notifications.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE823;"/>
                </tk:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="CrossSessionReplenishmentToggle"
                              AutomationProperties.Name="Notify about quota increases since the last session"
                              Toggled="OnCrossSessionReplenishmentToggled"/>
            </tk:SettingsCard>

            <tk:SettingsCard Header="Warning threshold" Description="Notify when a usage window reaches this consumed percentage.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE7BA;"/>
                </tk:SettingsCard.HeaderIcon>
                <NumberBox x:Name="WarningThresholdBox"
                           AutomationProperties.Name="Warning threshold"
                           Minimum="1"
                           Maximum="99"
                           SmallChange="1"
                           LargeChange="5"
                           SpinButtonPlacementMode="Compact"
                           ValueChanged="OnWarningThresholdChanged"
                           Width="120"/>
            </tk:SettingsCard>

            <tk:SettingsCard Header="Critical threshold" Description="Notify again when a usage window reaches this higher consumed percentage.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE814;"/>
                </tk:SettingsCard.HeaderIcon>
                <NumberBox x:Name="CriticalThresholdBox"
                           AutomationProperties.Name="Critical threshold"
                           Minimum="2"
                           Maximum="100"
                           SmallChange="1"
                           LargeChange="5"
                           SpinButtonPlacementMode="Compact"
                           ValueChanged="OnCriticalThresholdChanged"
                           Width="120"/>
            </tk:SettingsCard>

            <tk:SettingsCard Header="Notification cooldown" Description="Minimum minutes before TaskbarQuota repeats the same alert.">
                <tk:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE823;"/>
                </tk:SettingsCard.HeaderIcon>
                <NumberBox x:Name="AlertCooldownBox"
                           AutomationProperties.Name="Notification cooldown"
                           Minimum="1"
                           Maximum="1440"
                           SmallChange="1"
                           LargeChange="15"
                           SpinButtonPlacementMode="Compact"
                           ValueChanged="OnAlertCooldownChanged"
                           Width="120"/>
            </tk:SettingsCard>

            <TextBlock Text="Credentials" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,16,0,2"/>
            <TextBlock Text="All providers detect credentials automatically. If auto-detection fails for a provider, use the &quot;Fix&quot; button on its dashboard card."
                       Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="Wrap" Margin="0,0,0,4"/>

            <TextBlock Text="About" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,16,0,2"/>
            <tk:SettingsCard Description="Native WinUI widget for AI coding-tool usage.">
                <tk:SettingsCard.Header>
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Border Width="40" Height="40"
                                CornerRadius="4">
                            <Image Source="ms-appx:///Assets/TaskBarQuota.png" Stretch="UniformToFill"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center" Spacing="2">
                            <TextBlock Text="TaskbarQuota" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <TextBlock x:Name="VersionLabel"
                                       Opacity="0.6"
                                       Style="{StaticResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </tk:SettingsCard.Header>
            </tk:SettingsCard>

            <controls:UpdateActionBar x:Name="SettingsUpdateBar"
                                        Placement="Settings"/>

        </StackPanel>
    </ScrollViewer>
</Page>
