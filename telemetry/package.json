{
  "name": "taskbarquota-telemetry",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "deploy": "npx wrangler deploy",
    "schema": "npx wrangler d1 execute taskbarquota-telemetry --remote --file=schema.sql"
  }
}
